{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "node scripts/db-provider.mjs && prisma generate && prisma db push && next build"
}
